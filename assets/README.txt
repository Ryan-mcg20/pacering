Optional UI asset for realistic monitor heart background:

- Place a PNG at `assets/heart_realistic_bg.png`
- Recommended size: 1024x1024 with transparent background
- The app auto-detects this file and uses it behind the BPM readout
- If missing, the built-in vector animated heart is used as fallback
